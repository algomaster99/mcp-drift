name: Google Developer Knowledge MCP Lists

on:
  schedule:
    - cron: '0 4 * * *'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

env:
  MCP_URL: https://developerknowledge.googleapis.com/mcp

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version-file: go.mod
          cache: true

      - name: Build
        run: go build -o mcp-drift .

      - name: Fetch MCP lists (stateless — no session required)
        run: |
          mkdir -p live

          fetch_list() {
            local command="$1"
            local output="$2"

            if ./mcp-drift "$command" "$MCP_URL" > "$output" 2> error.log; then
              if [ "$(tr -d '[:space:]' < "$output")" = "null" ]; then
                printf '[]\n' > "$output"
              fi
              return
            fi

            if grep -qiE 'Method not found|Method not supported' error.log; then
              printf '[]\n' > "$output"
              return
            fi

            cat error.log >&2
            exit 1
          }

          fetch_list tools-list live/tools.json
          fetch_list prompts-list live/prompts.json
          fetch_list resources-list live/resources.json

      - name: Compare to snapshot
        id: diff
        run: |
          drifted=false

          for list in tools prompts resources; do
            if ! diff -q "snapshots/$list/google-developerknowledge.json" "live/$list.json" > /dev/null 2>&1; then
              echo "Drift detected in $list/list."
              drifted=true
            fi
          done

          echo "drifted=$drifted" >> $GITHUB_OUTPUT

      - name: Open PR with updated snapshot
        if: steps.diff.outputs.drifted == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          BRANCH="drift/google-developerknowledge-$(date +%Y-%m-%d)"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git checkout -b "$BRANCH"
          cp live/tools.json snapshots/tools/google-developerknowledge.json
          cp live/prompts.json snapshots/prompts/google-developerknowledge.json
          cp live/resources.json snapshots/resources/google-developerknowledge.json
          changed_lists=()
          for list in tools prompts resources; do
            if ! git diff --quiet -- "snapshots/$list/google-developerknowledge.json"; then
              changed_lists+=("$list")
            fi
          done
          python3 .github/scripts/bump_readme_counts.py README.md "[developerknowledge.googleapis.com/mcp]" "${changed_lists[@]}"
          git add README.md snapshots/tools/google-developerknowledge.json snapshots/prompts/google-developerknowledge.json snapshots/resources/google-developerknowledge.json
          git commit -m "drift: google-developerknowledge MCP lists changed $(date +%Y-%m-%d)"
          git push origin "$BRANCH"
          gh pr create \
            --title "drift: google-developerknowledge MCP lists changed ($(date +%Y-%m-%d))" \
            --body "One or more MCP lists at $MCP_URL changed. Review the snapshot diff in the files changed tab." \
            --base main \
            --head "$BRANCH"
